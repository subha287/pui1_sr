$('#flipbook').turn({
    width:1000,
    height:580,
    page:1,
    autoCenter:true,

    duration: 2000
    
    })

// this is the function to manipulate the properties of a "page" in the book function